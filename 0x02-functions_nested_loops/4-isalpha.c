#include "main.h"

/**
 * _isalpha - Return 1 if c is a letter. lowercase or uppercase
 *
 * Return: 1 if its alphabet otherwise 0
 */

int _isalpha(int c)
{
	if (c > 0)
		return (1);
	else
		return (0);
}
